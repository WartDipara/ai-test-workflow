# 已学技能：爱琳诗篇自动截图启动

### 推荐步骤顺序
- 按键精灵「快速引导」页 → 点击「写脚本(需ROOT)」(430,1575)
- 进入「我的腳本」→ 点击「未分类」(263,1314) 展开列表
- 确认脚本名「爱琳诗篇-免费版自动截图」（438~414）精确匹配后点击
- 进入配置页 → 点击「加载」(576,1810)
- 加载后页面出现「启动」按钮（540,1455）
- 点击「启动」→ 立即调用 `wait_for_game_after_script_launch`（90s超时）

### 关键 OCR 词
- 快速引导、写脚本(需ROOT)、我的腳本、未分类、1个脚本、爱琳诗篇-免费版自动截图、加载、加载中。、正在运行、启动

### 等待/广告处理
- 点击「加载」后短暂出现「加载中。」约0.8s，随后页面自动进入「正在运行」状态并显示「启动」按钮
- 点击「启动」后立即跳转至桌面/游戏中心界面（底部显示系统应用、按键精灵、小7手游、·爱琳诗篇标签），无需处理任何弹窗
- 由 `wait_for_game_after_script_launch` 阻塞检测游戏进程，第13次轮询（约26s）检测到 `com.xt.alsp35.x7sy` 启动

### 易错点
- 必须精确匹配脚本显示名「爱琳诗篇-免费版自动截图」，不点「分享脚本」「2MB」等相邻文字
- 必须先展开「未分类」分类（点击文字本身），再点击脚本名；不能直接点「1个脚本」或空白处
- 点击「加载」后若页面短暂显示「加载中。」，不要做任何操作，等待自动过渡到「正在运行」页
- 点击「启动」后必须立即调用游戏检测工具，不得手动点击或等待弹窗，页面跳转桌面属于正常启动流程

### 补充/差异（对照 SKILL.md）
- 标准流程中「加载」后期望出现「开始运行」提示，但本脚本加载后直接出现「启动」按钮，中间只有「加载中。」短暂过渡
- 标准流程加载后页面不变，本脚本加载后自动跳转为「正在运行」状态的新页面（显示环境/账号设置+启动按钮），坐标与加载前不同
- 点击「启动」后页面直接跳转至桌面/游戏中心（非游戏内界面），底部导航标签可见，属于正常启动行为
- 本设备已ROOT，可直接使用「写脚本(需ROOT)」入口，无权限阻断
